#!/usr/bin/env python3
"""Standalone entry point; resolving this file also supports a symlink on PATH."""

import os
import sys
from pathlib import Path

viewer = Path(__file__).resolve().parent / "experiments/workspace_viewer/viewer.py"
os.execv(sys.executable, [sys.executable, str(viewer), *sys.argv[1:]])
