#!/usr/bin/env python3
"""Try the compact UI with an isolated, persistent sample library."""

import os
import sys
from pathlib import Path

script = Path(__file__).resolve().parent / "scripts/ui_preview.py"
os.execv(sys.executable, [sys.executable, str(script), *sys.argv[1:]])
